{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="main landingpage" role="main">
        {{ content }}

        {% for page in pages("technology") %}
        <div class="tech-box">
            <h2>{{ page.title }}</h2>
            <p>{{ page.description }}</p>
            <href="{{ page.url }}">
                <a class="fas fa-arrow-circle-right fa-lg" href="{{ page.url }}" aria-label="arrow"></a>
            </a>
        </div>
        {% endfor %}
    </div>
    </div>

    {% include 'incl/footer.twig' %}
</body>

</html>